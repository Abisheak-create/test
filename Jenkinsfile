pipeline {
    agent any 
    stages {
        stage('Docker Build') { 
            steps {
                echo "This is Build stage. master"
            }
        }
        stage('Docker push') { 
            steps {
                echo "This is docker push stage. master"
            }
        }
    }
}
